document.getElementById("signupForm").addEventListener("submit", function (event) {
  event.preventDefault();
  // Logika untuk menangani pendaftaran dapat ditambahkan di sini
  alert("Form pendaftaran telah dikirim!");
});
